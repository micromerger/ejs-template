import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { <%=model.name%>Component } from './<%=model.name%>.component';

describe('<%=model.name%>Component', () => {
  let component: <%=model.name%>Component;
  let fixture: ComponentFixture<<%=model.name%>Component>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ <%=model.name%>Component ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(<%=model.name%>Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});