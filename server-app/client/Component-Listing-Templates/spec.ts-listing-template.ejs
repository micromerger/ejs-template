import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { <%=model.name%>ComponentListing } from './<%=model.name%>.listing.component';

describe('<%=model.name%>Component', () => {
  let component: <%=model.name%>ComponentListing;
  let fixture: ComponentFixture<<%=model.name%>ComponentListing>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ <%=model.name%>ComponentListing ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(<%=model.name%>ComponentListing);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});