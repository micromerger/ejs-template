<html>

<head>
</head>

<body>

    <h1><%= model.name %></h1>


    <%for (let test in model.properties){if (!model.properties.hasOwnProperty(test)) continue; var models = model.properties[test];%><input type=<%= models.type%> placeholder= <%= test%> ><br ><%} %>
    <button>Submit</button>
    
    
    </body>

</html>
